#pragma once

#define WORKSPACE_NAME "${meta:workspaceName}"
#define WORKSPACE_AUTHOR "${meta:workspaceAuthor}"
#define WORKSPACE_DESCRIPTION "${meta:workspaceDescription}"
#define WORKSPACE_HOMEPAGE "${meta:workspaceHomepage}"
#define WORKSPACE_LICENSE  "${meta:workspaceLicense}"
#define WORKSPACE_README  "${meta:workspaceReadme}"
#define WORKSPACE_VER  "${meta:workspaceVersion}"
#define WORKSPACE_VER_MAJOR "${meta:workspaceVersionMajor}"
#define WORKSPACE_VER_MINOR "${meta:workspaceVersionMinor}"
#define WORKSPACE_VER_PATCH "${meta:workspaceVersionPatch}"
#define WORKSPACE_VER_TWEAK "${meta:workspaceVersionTweak}"

#define PROJECT_NAME "${meta:name}"
#define PROJECT_AUTHOR "${meta:author}"
#define PROJECT_DESCRIPTION "${meta:description}"
#define PROJECT_HOMEPAGE "${meta:homepage}"
#define PROJECT_LICENSE "${meta:license}"
#define PROJECT_README "${meta:readme}"
#define PROJECT_VER "${meta:version}"
#define PROJECT_VER_MAJOR "${meta:versionMajor}"
#define PROJECT_VER_MINOR "${meta:versionMinor}"
#define PROJECT_VER_PATCH "${meta:versionPatch}"
#define PROJECT_VER_TWEAK "${meta:versionTweak}"
